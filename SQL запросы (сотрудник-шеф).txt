CREATE TABLE IF NOT EXISTS Chief (
id SERIAL PRIMARY KEY
);

 CREATE TABLE IF NOT EXISTS Employee (
id SERIAL PRIMARY KEY,
First_name VARCHAR(60) NOT NULL,
Departmen VARCHAR(60) NOT NULL,
Chief SERIAL REFERENCES Chief(id)
);